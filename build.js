const fs=require('fs');fs.mkdirSync('dist',{recursive:true});for(const f of ['index.html','app.js','favicon.svg','manifest.webmanifest'])fs.copyFileSync(f,'dist/'+f);const u=process.env.SUPABASE_URL||'';const k=process.env.SUPABASE_PUBLISHABLE_KEY||'';fs.writeFileSync('dist/config.js','export const SUPABASE_URL='+JSON.stringify(u)+';export const SUPABASE_PUBLISHABLE_KEY='+JSON.stringify(k)+';');